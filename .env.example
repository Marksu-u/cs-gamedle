# URL publique du site, sans slash final.
NEXT_PUBLIC_SITE_URL=https://strikedle.com

# Secret partagé de /api/revalidate
CRON_SECRET=

# Dev-only: set to 1 to keep the "reveal the answer" button on a preview
# deployment, where NODE_ENV is already "production". Never set it in prod.
NEXT_PUBLIC_DEV_TOOLS=